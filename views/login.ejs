<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="theme-color" content="#0f172a" />
        <title>Login – URL Shortener</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-indigo-900 flex items-center justify-center p-6">
        <div class="w-full max-w-md">
            <div class="bg-white/10 backdrop-blur-xl border border-white/20 rounded-2xl shadow-2xl p-8 text-white">
                <header class="mb-6">
                    <h1 class="text-2xl font-semibold tracking-tight">Welcome back</h1>
                    <p class="text-slate-300 text-sm mt-1">Sign in to continue to your dashboard</p>
                </header>

                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="mb-4 rounded-md bg-red-500/15 border border-red-500/30 p-3 text-sm text-red-200">
                        <%= error %>
                    </div>
                <% } %>
                <% if (typeof message !== 'undefined' && message) { %>
                    <div class="mb-4 rounded-md bg-emerald-500/15 border border-emerald-500/30 p-3 text-sm text-emerald-200">
                        <%= message %>
                    </div>
                <% } %>

                <form action="/user/login" method="POST" class="mt-6 space-y-5">
                    <div>
                        <label for="email" class="block text-sm mb-2 text-slate-200">Email</label>
                        <input
                            type="email"
                            id="email"
                            name="email"
                            placeholder="you@example.com"
                            required
                            class="w-full rounded-lg bg-white/10 border border-white/20 text-white placeholder-slate-400 px-4 py-2.5 outline-none focus:ring-2 focus:ring-indigo-400 focus:border-indigo-400 transition"
                        />
                    </div>

                    <div>
                        <label for="password" class="block text-sm mb-2 text-slate-200">Password</label>
                        <input
                            type="password"
                            id="password"
                            name="password"
                            placeholder="••••••••"
                            required
                            class="w-full rounded-lg bg-white/10 border border-white/20 text-white placeholder-slate-400 px-4 py-2.5 outline-none focus:ring-2 focus:ring-indigo-400 focus:border-indigo-400 transition"
                        />
                    </div>

                    <button
                        type="submit"
                        class="w-full inline-flex justify-center items-center rounded-lg bg-indigo-500 hover:bg-indigo-400 active:bg-indigo-600 disabled:opacity-60 text-white font-medium px-4 py-2.5 shadow-lg shadow-indigo-900/30 transition"
                    >
                        Sign in
                    </button>
                </form>

                <p class="mt-6 text-center text-sm text-slate-300">
                    Don't have an account?
                    <a href="/signup" class="font-medium text-indigo-300 hover:text-indigo-200 underline underline-offset-4">Create one</a>
                </p>
            </div>

            <p class="mt-6 text-center text-xs text-slate-400">
                &copy; <%= new Date().getFullYear() %> URL Shortener
            </p>
        </div>
    </body>
    </html>